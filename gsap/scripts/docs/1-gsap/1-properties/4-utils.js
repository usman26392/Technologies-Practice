



var filterProperty = gsap.utils.checkPrefix("filter");
console.log(filterProperty);

// var clampNumber = gsap.utils.clamp(0, 100, 98);
var clampNumber = gsap.utils.clamp(5 , 50, 70);
console.log(clampNumber);