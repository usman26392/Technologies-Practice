/**
 * default constructor:
 * jub hum constructor may koi instance declare nai kartay
 * tab default constructor develop hota hay.
 * jub hum apna constructor declare kartay hayn toh ,hammara constructor
 * default constructor ko override kar dayta hay.
 */

class Mobile {
   //  prototype members of Mobile.
   display() {
       console.log("prototype method of Mobile object. ");
   }
   model = "1130";
}

let samSung = new Mobile();